version: '2.1'
orbs:
  codecov: codecov/codecov@3

jobs:
  build:
    docker:
      - image: circleci/python:3.8
    environment:
      PYTHONPATH: /bmiProject/BMIfinder
      DJANGO_SETTINGS_MODULE: BMIfinder.settings
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: |
            python -m venv venv
            . venv/bin/activate
            pip install -r requirements.txt
            pip install pytest
      - run:
          name: Run tests
          command: |
            . venv/bin/activate
            pytest -v -s
            #python manage.py test
      - run: pytest --cov=bmiCalc tests/
      - codecov/upload:
          token: env.CODECOV_TOKEN

workflows:
  build-and-test:
    jobs:
      - build
